# Copy to `.env` and fill your credentials. Python scripts in this repo
# will auto-load `.env` via sitecustomize.py (no need to `source` for Python).

ZOTERO_USER_ID=1234567
ZOTERO_API_KEY="your_zotero_api_key"
ZOTERO_STORAGE_DIR="$HOME/Zotero/storage"

# Doubao / ByteDance Ark
ARK_API_KEY="your_ark_api_key"
# Optional: Use a specific bot model id (defaults to a built-in when absent)
# ARK_BOT_MODEL="bot-xxxxxxxxxxxxxxxx"

# Qwen / DashScope (OpenAI-compatible)
# AI_PROVIDER="qwen"
# DASHSCOPE_API_KEY="your_dashscope_api_key"
# DASHSCOPE_MODEL="qwen3-max"

# Generic OpenAI-compatible endpoint (including official OpenAI)
# AI_PROVIDER="openai"
# AI_API_KEY="sk-..."
# AI_BASE_URL="https://api.openai.com/v1"
# AI_MODEL="gpt-4o-mini"

# Notion
# NOTION_API_KEY="your_notion_api_key"
# NOTION_DATABASE_ID="your_database_id"

# Google Drive (service account or OAuth client)
# GOOGLE_DRIVE_ROOT_FOLDER="drive_folder_id"
# GOOGLE_SERVICE_ACCOUNT_FILE="/path/to/service-account.json"
# GOOGLE_OAUTH_CLIENT_FILE="/path/to/client_secret.json"
# GOOGLE_OAUTH_TOKEN_FILE="$HOME/.config/zotero-drive-oauth/token.json"

# Optional proxies
# HTTP_PROXY="http://127.0.0.1:7890"
# HTTPS_PROXY="http://127.0.0.1:7890"
# NO_PROXY="localhost,127.0.0.1"
